# 2026-07-29 세션 진행 기록

**시작 상태**: v1.5.0 (로컬 = origin/main)
**종료 상태**: v1.5.1 — 별표 라인 고정폭 정렬로 송장 수량 밀림 해결

## 1. 위드유 파일변환 테스트 (오전)

- `새 폴더`(`SABANGNET_WORK_FOLDER` override)의 발주서/재고/위치 3파일로 `_run_new_flow.py --from-precheck --stop-after-precheck` 반복 실행
- 위드유 실행/로그인 → 파일변환 → 정제주소 프리체크까지만 (택배정보생성/송장출력 클릭 X)
- 파이썬은 `python` 아님 → `py` 런처(3.14)로 실행해야 함 (PATH에 python 없음)
- 정제 오류 시 in-memory 필터(4→3, 1792→1791) 후 재변환 → 통합.xlsx 덮어쓰기 + 출고리스트 재생성 확인. 수량 집계는 삭제 반영본 기준(발주서 5431 → 통합 5430, 차이 1) 검증

## 2. v1.5.1 별표 라인 고정폭 정렬 (송장 수량 한 칸 밀림 해결)

### 문제 (사용자, 20260729 라벨)
- 송장 별표 라인 `★★★ [N] 일반 합포 잔여 ★★★`이 **가끔 한 칸 밀려서** 출력
- 원인 분석: 프린터 무죄. **엑셀 데이터 문제** — 별표 라인이 셀 안 공백 정렬 텍스트인데, 고정 "칼럼 폭"이 아니라 고정 "공백 개수"(`[N]` 뒤 12칸, 값 사이 6칸)로 조립됨 → 숫자 자릿수가 바뀌면 뒤 칼럼이 통째로 밀림
- 실측: 20260729 통합 파일 169개 별표 라인 중 **25개(15%)가 v1 시작위치 20(정상 19)** 으로 밀려 있었음. `[N]` 2자리(최대 [76]) 25개, 잔여 3자리 59개가 유발

### 왜 오늘만? (사용자 질의)
- 밀림 버그는 **v1.1.4(2026-06-04)** `[N]` 전체 회사 합산 총수량 도입 + **v1.1.5** 12칸 고정 간격 때부터 잠재. v1.4.5(2026-07-15)에서 수량값도 전역 합산 → 값이 더 커짐
- 다자리 숫자가 있어야만 드러나는데, **오늘 배치가 대량(1792건)** 이라 2자리 [N]·3자리 잔여가 다발 → 밀린 라벨이 여러 장 눈에 띔. 소량 배치 날은 우연히 전부 1자리라 정렬이 맞아 보였을 뿐

### 결정 (사용자)
- **A안: 각 칼럼을 고정 폭으로 패딩** (다른 옵션: 구분자/라벨 표기 기각)

### 구현 ([main.py:503~](main.py#L503) 헬퍼 신규, 5개 포맷 교체)
- `_star_visual_width`(동아시아 전각=2), `_star_pad`(좌측정렬 고정폭, 최소 1칸), `_format_star_line(bracket, v1, v2, v3)` 추가
- 폭 상수: `_STAR_BRACKET_W=15` (`[3]`+12), `_STAR_VALUE_W=7` (`3`+6) → **1자리 기준 기존 간격 그대로 보존**
- 별표 라인 5개 지점(해외/훼손/합포만/일반만/일반+합포)을 `_format_star_line` 호출로 교체
- 미사용 죽은 변수 `total_blank` 제거 (훼손 bracket은 `''` 넘겨 처리)
- 참고: 별표 폭 추적(`code_star_w_local`/`_vw`, main.py:580·651-)은 쓰기만 하고 읽는 곳 없는 죽은 코드 → 포맷 변경 안전. 별표 라인 파싱 타 코드는 전부 `'★★★' 포함 여부`만 검사 → 포맷 무관

### 검증
- 1자리 순수 케이스 바이트 동일 (하위호환)
- 169개 별표 라인: v1 [19,20]→[19], v2 [26,28]→[26], v3 [33,35,36]→[33] **전 칼럼 고정 위치 통일** (밀림 0)

### 릴리스
- version.txt 1.5.0 → 1.5.1
- 커밋 + 태그 v1.5.1 push → GitHub Actions(`v*` 트리거) 자동 빌드/릴리스

### 미결
- 미러 재복사: 사방넷 `C:\sabangnet\sabangnet\송장파일 생성` (현재 v1.5.0) → v1.5.1 재복사 필요 여부 사용자 확인
- 라벨 셀 폭보다 라인이 길면 여전히 줄바꿈 발생 가능(고정폭이라 이제 일관됨). 필요 시 라인 단축(옵션 C) 별도 검토

---

# 2026-07-15 세션 진행 기록

**시작 상태**: 로컬 v1.1.1 (원격 v1.4.4보다 19커밋 뒤처짐, 로컬 미커밋 초안 있음)
**종료 상태**: v1.4.8 릴리스 완료, 로컬 = origin/main 동기화

## 1. 저장소 정리

- 로컬 미커밋 초안(포장 라인 기능)은 원격 v1.2.0에 이미 상위 버전으로 반영되어 있어 폐기 대상
- `git stash -u`로 초안 백업 후 `git pull` fast-forward → 로컬 v1.4.4 정합
- 백업 stash: `stash@{0}: v1.1.1 base 포장 라인 초안`

## 2. v1.4.5 별표 라인 값 전역 합산화 (커밋 695829b 이전 단계)

### 문제
- **119207-0001** (관절연골 콘드로이친): 6행 = 제이제이몰 7 + 3PL 3 = **총 10**
- 통합 파일 별표 라인 표시: `★★★ [10]  7  0  0  ★★★`
- `[10]`은 정확하나 회사별 자리값은 **첫 등장 시트(제이제이) 로컬 값 7만** 반영 → 총수량과 3만큼 불일치

### 원인
- `save_sheets` 시절 로컬 defaultdict 4개(normal_qty/happo_qty/overseas_qty/damaged_qty)로 시트별 집계하던 코드가 통합 방식(v1.3.0) 도입 후에도 남아있어 별표 라인 자리값에 사용됨
- 통합 후 상품코드별 첫 등장 행만 별표 유지 → 첫 시트의 로컬 계산값만 남는 구조적 문제

### 결정 (사용자)
- **전체 회사 합산 한 줄**로 표시 (다른 옵션: 회사별 여러 줄 / 회사명 접두 병기 → 기각)

### 구현
- 로컬 defaultdict 4개 + 시트 집계 for 루프 삭제 (죽은 코드)
- 별표 라인 계산 4개 지점을 `total_*_qty` 전역 값 참조로 교체:
  - main.py:584 `o = total_overseas_qty.get(code, 0)`
  - main.py:597 `d = total_damaged_qty.get(code, 0)`
  - main.py:608-609 `n = total_normal_qty`, `h = total_happo_qty`
- 케이스 판정(`n==0`, `h==0`) 조건이 자동으로 전역 기준이 되면서 **부수적 버그(합포 행 오표시)도 자동 해결**

### 검증
- 119207-0001: `★★★ [10]  10  0  remaining  ★★★` ✅ 자리값 합 = [N]
- 122797-0001 (콘드로이친): 이전엔 합포 행에 잘못 표시(`[16] 0 12`)되던 것이 규칙대로 일반 행으로 이동 (`[16] 16 12 remaining`)

## 3. v1.4.6 유형별 별표 유지

### 문제
- **20260511 파일 125356-0001** (센텔리안24 앰플): 해외 1개 + 일반 13개 = **총 [14]**
- 정렬상 해외 행 첫 등장 → 그 별표만 남고 일반 별표는 삭제됨
- 표시: `★★★ [14]  [해외] 1  0  226  ★★★` → 일반 13이 어디에도 안 보임

### 결정 (사용자)
- **옵션 B: 유형별 별표 각각 유지** (해외 별표 하나 + 일반 별표 하나)

### 구현
- `save_integrated` 중복 제거 로직 [main.py:725-747](main.py#L725)
- `seen_stars`를 상품코드 → **(상품코드, 유형)** 튜플로 변경
- 유형 판정: overseas / damaged / normal 3종 (is_overseas, is_damaged 함수 활용)

### 검증
- 125356-0001:
  - 해외 행: `★★★ [14]  [해외] 1  0  -14  ★★★`
  - 일반 첫 등장: `★★★ [14]  13  0  -14  ★★★`
- 두 별표 [N] 동일, 자리값 합 = 14 ✅

## 4. v1.4.7 위드유 합포 오판정 회피

### 문제
- 위드유(WithU) 송장 프로그램은 **수화주명 + 주소**만 보고 합포 판정 (송하인 무시)
- 같은 사람이 같은 주소로 서로 다른 회사에 주문한 경우 → 위드유가 하나의 합포 송장으로 오묶음
- 실제 사례: **20260511 김윤희 진주 주소** = *3pl물류 2건(탈취제 합포) + 비에이치 1건(백세알부민) 걸침

### 결정 (사용자)
- **수화주명 뒤에 `(회사명)` 태그 부착**
- **충돌 케이스만** (같은 수화주+주소가 여러 회사에 걸친 경우) — 정상 케이스는 원본 그대로
- 회사명에서 `*` 접두 제거 (예: `*3pl물류` → `(3pl물류)`)

### 구현
- `save_integrated` 별표 중복 제거 이후, 통합 시트 쓰기 이전에 삽입 ([main.py:749-777](main.py#L749))
- (수화주, 주소) → 등장 시트명 집합. 집합 크기 ≥ 2이면 충돌 케이스로 판정 → 태그 부착

### 검증
- 김윤희 진주 3행: `김윤희(3pl물류)`, `김윤희(3pl물류)`, `김윤희(비에이치)` ✅
- 김윤희 대전/용인/안동 (단독 회사): 태그 없이 원본 유지 ✅
- 전체 파일 2,033 조합 중 충돌 1건만

## 5. v1.4.5~v1.4.7 릴리스

- version.txt 1.4.4 → 1.4.7
- 커밋: `695829b v1.4.7 별표 라인 개선 + 위드유 합포 오판정 회피`
- 태그: `v1.4.7`
- 원격 push 완료 → GitHub Actions 자동 빌드/릴리스 트리거

## 6. v1.4.8 출고리스트 합포수량 병기

### 요구사항 (사용자)
- STEP 5 출고리스트 총출고/회사별 컬럼에 합포수량 괄호 병기
- 예: 총 5개 중 합포 2개 → `5(2)`
- 합포 0인 상품은 그대로 정수만 표시

### 구현
- [main.py:1934-1979](main.py#L1934) `_run_shiplist` 함수 내
- `_fmt_qty(qty, happo)` 헬퍼 함수 추가
- 총출고 자리, 회사별 자리 모두 `_fmt_qty(qty, happo)` 호출
- `total_happo` 전체 회사 합포 집계 추가

### 검증
- `_fmt_qty` 단위 테스트 통과 (7개 케이스 all pass)

### 릴리스
- version.txt 1.4.7 → 1.4.8
- 커밋: `37d7536 v1.4.8 출고리스트 총출고/회사별 컬럼에 합포수량 병기`
- 태그: `v1.4.8`
- 원격 push 완료 → GitHub Actions 자동 빌드/릴리스 진행 중

## 7. v1.4.9 전역 유형 집계를 재평가 후로 이동

### 문제 (사용자 지적)
- v1.4.8까지도 원본 파란색 상품이 재평가로 일반 강등된 후에도 `total_happo_qty`에는 여전히 카운트되어 있었음
- 별표 라인 자리값이 재평가 결과와 불일치 (`[15]  15  2` — 사실 합포 0인데 2로 표시)
- 출고리스트 총출고 병기도 잘못된 합포수량 사용

### 사용자 규칙 (재정립)
- **회사가 다른 파란색 묶음 = 실질적 합포 아님 = 완전 일반 처리**
- 별표 라인 합포 자리와 출고리스트 합포수량 모두 재평가 결과 반영해야

### 구현
- [main.py:504-541](main.py#L504) 전역 유형별 집계 블록을 **회사별 재평가 뒤로 이동**
- 순서만 변경 (재평가 뒤에 집계 → 강등된 값이 total_happo_qty 대신 total_normal_qty에 편입)
- [N] 총합 / 잔여재고 총합은 변화 없음 (유형 분류만 정확해짐)

### 검증
- 20260716 신영순 매스틱 케이스: `[15] 15 2` → `[15] 15 0` 정정

### 릴리스
- version.txt 1.4.8 → 1.4.9
- 커밋: `afe4312 v1.4.9 전역 유형별 집계를 회사별 합포 재평가 후로 이동`
- 태그: `v1.4.9`
- 원격 push 완료

## 8. v1.5.0 위치 파일에 특이사항 컬럼 추가

### 요구사항 (사용자)
- 뽁뽁이/안전포장/쇼핑백 표시 필요
- 고객이 봐도 거부감 없는 위장된 코드 사용
- 상품 하나당 하나만 입력하면 **모든 수량 행에 자동 반영**
- 기존 박스크기(극소/보냉팩) 뒤에 다이아로 구분해 표시

### 결정된 코드 규칙
- `AC` = 뽁뽁이 (Air Cap)
- `SP` = 안전포장 (Safe Pack)
- `SB` = 쇼핑백 (Shopping Bag)

### 표시 형식
| 케이스 | 표시 |
|---|---|
| 박스크기만 (기존) | `◇극소◇` |
| 특이사항만 | `◇AC◇` |
| 조합 | `◇극소◇AC◇` (다이아로 구분) |

### 구현
- [main.py:440-482](main.py#L440) `load_location_file` — `특이사항` 컬럼 파싱 + `special_map` 반환 (반환값 3튜플)
- [main.py:488](main.py#L488) `process_data` 시그니처에 `special_map=None` 인자 추가
- [main.py:652-671](main.py#L652) 표시 로직 통합: package(수량 1~12만) + special(수량 무관) 조합 처리
- [main.py:1779-1798](main.py#L1779) `App._run_process` 반환값/전달 인자 확장

### 규칙
- 특이사항은 **수량 무관** (상품 특성이므로 모든 수량 행에 표시)
- 합포 행은 **제외** (기존 박스크기와 동일)
- 훼손/해외 여부와 상관없이 매핑에 있으면 표시

### 검증 (20260429 파일, 114813-0001)
- 아무것도 없음: 표시 안 함 ✅ (하위 호환)
- package_only: `◇극소◇` ✅ (기존 동작)
- special_only: `◇AC◇` ✅ (수량 1, 2 모든 행)
- combined: `◇극소◇AC◇` ✅ (다이아 구분)

### 릴리스
- version.txt 1.4.9 → 1.5.0
- 커밋: `980fec9 v1.5.0 위치 파일에 특이사항 컬럼 추가`
- 태그: `v1.5.0`
- 원격 push 완료 → GitHub Actions 자동 릴리스

## 9. 미결/보류 사항

| 항목 | 상태 |
|---|---|
| 시트 분리 로직 정리 (통합 방식에서 낭비 계산) | 현상 유지 (합포 재평가에 시트 그룹화 필수, 리팩터링 위험 회피) |
| 해외+일반 혼합 케이스 별표 라인 | v1.4.6으로 유형별 유지 대응 완료 |
| 위드유 태그 예외 케이스 (특수문자, 회사명 규칙 확장) | 관찰만, 필요 시 대응 |
| 훼손 별표 라인 잔여재고 중복 표시 | 현상 유지 (각 라인 독립 정보 블록) |

## 10. 결과물

- **로컬 exe**: `dist/발주서처리프로그램.exe` (v1.5.0, 17:39 빌드)
- **원격 릴리스**: v1.4.7, v1.4.8, v1.4.9, v1.5.0 네 개 태그 push 완료
- **자동 업데이트**: 이하 사용자에게 v1.5.0 알림 예정
- **PR/이슈**: 없음 (직접 main 커밋)

## 11. 관련 memory 파일

- [MEMORY.md](file:///C:/Users/wonde/.claude/projects/C--Users-wonde-Desktop---------------/memory/MEMORY.md) — 인덱스
- [project_release.md](file:///C:/Users/wonde/.claude/projects/C--Users-wonde-Desktop---------------/memory/project_release.md) — 릴리스 요약
- [project_star_line_total.md](file:///C:/Users/wonde/.claude/projects/C--Users-wonde-Desktop---------------/memory/project_star_line_total.md) — 별표 라인 상세
- [project_withyou_handling.md](file:///C:/Users/wonde/.claude/projects/C--Users-wonde-Desktop---------------/memory/project_withyou_handling.md) — v1.4.7 위드유 태그
